// Minimal test — no imports beyond the assertion helpers
@import("std/test")

func add(a: Int, b: Int) -> Int {
    return a + b
}

func main() -> Int {
    let x: Int = 42;
    let y: Int = add(x, 10);
    assert_eq("add(42, 10)", y, 52);
    return 0
}
